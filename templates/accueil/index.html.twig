{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!
{% endblock %}

{% block body %}
	{{ dump(Services) }}
	<div class="container text-center">
		<div class="row">
			<div class="col-4">
			{# {% for service in Services %} #}
				<div class="card">

					{# <img src="imagesServices/{{ Service.picture }}"  #}
					<div class="card-body">
						{# <p class="card-text"></p>{{Service.title }} #}
						</div>
					</div>
				</div>
			</div>
			{# {% endfor %} #}